{
  "type": "object",
  "properties": {
    "action": { 
      "type": "string", 
      "enum": ["toggle_music", "toggle_flashlight"],
      "description": "System action: toggle music (Play/Pause) or flashlight." 
    }
  },
  "required": ["action"]
}