{
  "type": "object",
  "properties": {
    "category": {
      "type": "string",
      "enum": ["Projects", "Areas", "Resources", "Archives"],
      "description": "The PARA category for saving."
    },
    "content": {
      "type": "string",
      "description": "Synthesized text of the note, task, or extracted information."
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of tags for subsequent vector search."
    }
  },
  "required": ["category", "content"]
}